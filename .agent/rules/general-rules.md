---
trigger: always_on
---

Canonical agent instructions live in `AGENTS.md`.

- Use `AGENTS.md` as the single source of truth for rules.
- Use `.agent/workflows/*.md` for operational workflows only.
