appId: com.langdk.ultimatestats.dev
name: Seed Advanced Tracker Game
---
- launchApp
- openLink: ultimatestats-dev:///__maestro_seed__?trackerState=${TRACKER_STATE}
- retry:
    maxRetries: 1
    commands:
      - extendedWaitUntil:
          visible:
            id: 'maestro-setup-running'
          timeout: 5000
- retry:
    maxRetries: 1
    commands:
      - extendedWaitUntil:
          notVisible:
            id: 'maestro-setup-running'
          timeout: 15000
- extendedWaitUntil:
    visible:
      id: 'tracker-menu-button'
    timeout: 15000
